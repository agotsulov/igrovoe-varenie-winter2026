// Eye configuration
radius = 8;           // Radius for pupil movement
follow_speed = 0.15;   // Smooth follow speed (0-1)
pupil_offset_x = 0;    // Horizontal offset from center
pupil_offset_y = 0;    // Vertical offset from center
max_pupil_distance = 2; // Max distance pupil can move from center

// Current pupil position (relative to origin)
pupil_x = 0;
pupil_y = 0;


phase = 1